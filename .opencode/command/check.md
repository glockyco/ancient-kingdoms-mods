---
description: Run full website validation (check, lint, build)
---

Run the website validation suite:

```bash
cd website && pnpm check && pnpm lint && pnpm build
```

If there are errors, analyze them and suggest fixes.
