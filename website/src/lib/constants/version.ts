/**
 * Game version the compendium data was exported and built from.
 *
 * Update when bumping to a new game patch (see skill: update-game-version).
 * The home-page banner renders this in the "Updated for v..." segment and
 * compares it against the live game version fetched from Steam to flag the
 * compendium when it is behind.
 */
export const COMPENDIUM_VERSION = "0.9.19.2";
