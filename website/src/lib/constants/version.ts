/**
 * Game version the compendium data was exported and built from.
 *
 * Update both values when bumping to a new game patch (see skill:
 * update-game-version). The home-page banner reads these to render the
 * "Updated for v..." line and to compare against the live game version
 * fetched from the /api/game-version worker route.
 */
export const COMPENDIUM_VERSION = "0.9.14.3";
export const COMPENDIUM_DATE = "2026-04-29"; // ISO date of the patch release
