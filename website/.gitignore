node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/.sitemap-manifest.prev.json

# Generated files (from build-pipeline)
# The databases are build inputs, not published files. They live outside
# static/ so they are never served verbatim at a stable, unhashed URL; the
# client copies are gzipped and content-hashed into the Vite asset graph.
/data/
/static/tiles/
/static/images/
/static/sitemap-manifest.json
/static/mods/
/static/.mods-*/
/static/mods.old-*/
# src/lib/generated/home-counts.ts is committed: it is a derived artifact the
# home page imports, and without it svelte-check, vitest and knip cannot run on
# a clean checkout. Regenerated by scripts/generate-home-counts.mjs on prebuild.

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
