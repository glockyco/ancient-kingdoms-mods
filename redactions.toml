# Items carrying ignore_journal=true are always excluded, together with every
# entity left with no other source. The game marks them as internal, so there is
# no configuration to switch this off.

# Items where crafting information should be hidden.
# The item itself remains visible, but:
# - crafted_from field is cleared
# - used_in_recipes entries for recipes producing this item are removed
# - Materials don't show "used to craft X" for these recipes
[items.hide_crafting]
ids = [
    "divine_essence",
    "draconium_wyrmguard",
    "draconium_scaleplate",
    "draconium_crownhelm",
    "draconium_wyrmtreads",
    "draconium_scalegrasp",
    "steel_of_ancient_dragons",
    "valaark_ancient_coin",
]

# Zones in which the game itself withholds positional information from the
# player. Every entity of the zone stays published. All of its geometry is
# removed, so the compendium discloses no more than the game does.
#
# Temple of Valaark: the client closes the live map and substitutes a static
# illustration, disables the player marker, refuses teleport items, and returns
# a player who enters to their bind point.
[zones.suppress_positions]
zone_ids = ["temple_of_valaark"]

# Zones that have not shipped. Everything related to the zone is removed: its
# rows, every row referencing it, and every entity left with no other source.
[zones.exclude_unreleased]
zone_ids = ["old_valorath"]

# Entities of unreleased content that hold no reference to a redacted zone and
# no source or usage of any kind. Nothing in the data distinguishes them from
# unconnected content that is deliberately published, so they can only be named.
#
# old_valorath_token: no source, no usage, no zone reference. It is one of 82
# items with neither source nor usage, several of which are published on
# purpose, so no derivable rule can select it.
[entities.exclude]
ids = ["old_valorath_token"]

# Quests to completely exclude from the database.
# These quests won't appear in any listings or relationships.
[quests.exclude]
ids = [
    "relics_of_the_ancient_faith",
    "the_lost_warden",
]
